## Bootstrap

1. Confirm environment health.

```powershell
peeku doctor
```

2. Start daemon mode for low-latency command loops and `watch`.

```powershell
peeku --daemon
```

3. Stop daemon when needed.

```powershell
peeku --daemon --stop
```
